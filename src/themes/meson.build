# Install bundled theme preset JSONs into the Aegisub data directory.

themes = files(
  'aegisub_default.json',
  'ayu_dark.json',
  'ayu_light.json',
  'dark_mode_unofficial.json',
)

install_data(
  themes,
  install_dir: dataroot / 'themes',
)
